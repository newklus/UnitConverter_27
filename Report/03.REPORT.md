# UnitConverter_27 — 세션 3 Harness · Rules · TDD RED (T1)

| 항목 | 내용 |
|------|------|
| **프로젝트** | UnitConverter_27 — meter 기준 길이 단위 변환기 |
| **단계** | 세션 3 (README Activities 3 · TC 0.5h) |
| **보고서 생성일** | 2026-06-11 |
| **목적** | Harness·SSOT·TDD RED 착수 및 세션 산출물·다음 Phase 정리 |

**관련 Transcript:** [Prompting/03.Export-Transcript.md](../Prompting/03.Export-Transcript.md)

---

## 1. 요약

Mom Test STEP 3 워크북을 기준으로 **pytest Harness**, **`unit_convert` API 계약**, **`.cursorrules` SSOT(Rule ①~⑧)**, **Cursor Command**(`/tdd-red`, `/export`)를 구축했다. API 명칭은 `validate_lines`/`grid`에서 **`unit_convert(user_input: float)`** 로 정리했고, **T1 RED**(`user_input=-1` → `status="fail"`)를 `tests/`에 고정·pytest **FAILED 1** 확인했다. `src/unit_convert.py`는 스텁 유지(GREEN 대기).

---

## 2. 핵심 결정·산출물

| 구분 | 결정·산출 | 비고 |
|------|-----------|------|
| **API** | `unit_convert(user_input: float) → {status, failed_lines}` | meter 숫자 입력 · pass/fail/incomplete |
| **SSOT** | `tests/` golden > `.cursorrules` > README > 워크북 | 표시 1자리 / 정밀 TC 분리 |
| **T1 RED** | `-1` → `fail`, `failed_lines=[{reason: negative_input, user_input: -1}]` | tests SSOT |
| **Harness** | `pyproject.toml`, `src/`, `tests/` | pytest `pythonpath=["src"]` |
| **Rules** | `.cursorrules` (~120줄) | 규칙·Command·구현·테스트 8계층 |
| **Command** | `.cursor/commands/tdd-red.md`, `export.md` | RED 전용 · Export 규칙 |
| **Export 이력** | `Report/01~02.REPORT.md`, `Prompting/01~02.Transcript.md` | 본 Export = **03** |

**명칭 이력:** `validate_lines` → `unit_convert` · `grid` → `user_input`(meter float)

**pytest (T1):** `test_t1_negative_input_fail` — FAILED 1 (스텁 `None` → TypeError, 의도된 RED)

---

## 3. 다음 단계

1. **GREEN** — `src/unit_convert.py`: `user_input < 0` 시 T1 dict 반환
2. **RED 추가** — `user_input=2.5`, `1` pass golden 3줄 TC
3. **환경** — `.venv` pytest 설치 · PowerShell `Activate.ps1` ExecutionPolicy 또는 `activate.bat`
4. **CLI TC** — `tests/test_unit_converter.py` (예정) + `UnitConverter.py` 1자리·음수

---

*본 문서는 Report/03.REPORT.md — UnitConverter_27 세션 3 Harness·Rules·TDD RED(T1) Export 요약입니다.*
