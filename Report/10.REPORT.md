# UnitConverter_27 — T2 RED Test Plan (`/red-test-plan`)

| 항목 | 내용 |
|------|------|
| **프로젝트** | UnitConverter_27 — meter 기준 길이 단위 변환기 |
| **단계** | 세션 3 · EXPORT (ARRR A단계 — T2 RED 설계) |
| **보고서 생성일** | 2026-06-11 |
| **순번** | **10** |
| **목적** | `unit_convert(2.5)` 표시 golden 3줄 pass에 대한 C2C·Track B·테스트 플랜 문서화 |

**관련 Transcript:** [Prompting/10.Transcript.md](../Prompting/10.Transcript.md)

---

## 1. 요약

`/red-test-plan`으로 **T2**(`meter:2.5` → 3줄 1자리 표시 golden pass) RED 묶음을 설계했다. 구현·tests 변경 없이 C2C·Track B·pytest 플랜·ECB/Mock 점검만 산출했으며, T1은 기존 RED(`test_t1_negative_input_fail`) 유지.

---

## 2. 핵심 결정·산출물

### STEP: RED (Ask — 설계만)

| Test ID | 함수 (예정) | 상태 | Command |
|---------|-------------|------|---------|
| T1 | `test_t1_negative_input_fail` | **FAILED** (의도 RED) | (기존 `/tdd-red` 완료) |
| **T2** | `test_t2_meter_2_5_display_pass` | **플랜만** — 미작성 | `/red-test-plan` |

### T2 C2C 요약

| 항목 | 내용 |
|------|------|
| FR | FR-DISPLAY (Rule ③·⑦) + FR-API (Rule ⑧) |
| Given | `user_input = 2.5` |
| Then | `status == "pass"` · `failed_lines == []` · `lines` 3줄 golden 일치 |
| Golden 3줄 | `2.5 meter = 2.5 meter` · `2.5 meter = 8.2 feet` · `2.5 meter = 2.7 yard` |

### 산출물 (문서)

| 구분 | 내용 |
|------|------|
| 채팅 | C2C 표 · Track B · 테스트 플랜 · ECB·Mock 점검 4블록 |
| 코드 | **변경 없음** (Export 금지 준수) |

---

## 3. 계약·Rule

- SSOT: `tests/` golden > `.cursorrules` > README · PRD 없음 → Rule ①~⑧·API 매핑
- API: `unit_convert(user_input: float) → {status, failed_lines}` · pass 시 `lines` 3줄 (tests SSOT)
- 표시 1자리 `round(,1)` · 정밀 TC 분리 (Rule ⑤)
- RED ③: Domain mock·E001~E005 emit **금지**

[STEP3 워크북](STEP3_세션3_워크북.md) — 링크만 (복붙 금지).

---

## 4. 구현 상태

| 파일 | Phase |
|------|-------|
| `src/unit_convert.py` | RED 스텁 (`...` → `None` 반환) |
| `tests/test_unit_convert.py` | T1 RED assert만 존재 |
| `UnitConverter.py` | (boundary — 미착수) |

---

## 5. 테스트 상태

`python -m pytest tests/ -v` — **Step A 실측** 2026-06-11

| 결과 | 상세 |
|------|------|
| **0 passed, 1 failed** | collected 1 · FAILED: `test_t1_negative_input_fail` (`TypeError: 'NoneType' object is not subscriptable`) |

---

## 6. 다음 단계

1. **`/red-skeleton`** — `test_t2_meter_2_5_display_pass` 골격 추가 (`pytest.fail`)
2. **`/tdd-red`** — T2 assert 본문 (`status` · `failed_lines` · `lines` golden)
3. **T1 GREEN** — `/green-minimal` (별도 1묶음) · 음수 fail 구현
4. **T3 RED** — `meter:1` 표시 golden (후속 플랜)

---

*본 문서는 Report/10.REPORT.md — T2 RED Test Plan Export입니다.*
